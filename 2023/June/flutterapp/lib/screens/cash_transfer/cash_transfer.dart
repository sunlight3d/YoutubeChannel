import 'package:flutter/material.dart';

class CashTransferScreen extends StatelessWidget {
  const CashTransferScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('CashTransferScreen Screen'),
    );
  }
}